# app-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
        - name: app
          image: ton_image_php_apache
          env:
            - name: DB_TYPE
              value: mysql
            - name: DB_HOST
              value: postgres
            - name: DB_NAME
              value: gestion_produits
            - name: DB_USERNAME
              value: root
            - name: DB_PASSWORD
              value: root
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: prod
spec:
  selector:
    app: app
  ports:
    - port: 80
      targetPort: 80
